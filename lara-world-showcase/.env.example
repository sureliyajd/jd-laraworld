# Frontend Configuration
# ======================
# Base path for the application (required for GitHub Pages deployment)
# Leave empty or "/" for root domain deployment
# Set to "/repository-name/" for GitHub Pages
VITE_BASE_PATH=/jd-laraworld/

# Frontend URL (optional - auto-detected if not set)
# Only set this if you need to override the auto-detected URL
# Examples:
#   Local: http://localhost:8080
#   Production: https://yourdomain.com
#   GitHub Pages: https://username.github.io/repo-name
# VITE_FRONTEND_URL=

# Backend API Configuration
# =========================
# Backend API base endpoint (with /api suffix)
# This is where all API requests will be sent
# Examples:
#   Local: http://localhost:8000/api
#   Production: https://api.yourdomain.com/api
#   Render/Vercel: https://your-app.onrender.com/api
VITE_API_BASE_ENDPOINT=http://localhost:8000/api

# Backend Base URL (without /api suffix)
# Used for broadcasting/auth endpoints and file downloads
# Examples:
#   Local: http://localhost:8000
#   Production: https://api.yourdomain.com
#   Render/Vercel: https://your-app.onrender.com
VITE_API_BASE=http://localhost:8000

# Pusher Configuration (Optional - for real-time notifications)
# =============================================================
# If not configured, real-time notifications will be in demo mode
# VITE_PUSHER_APP_KEY=your-pusher-key
# VITE_PUSHER_APP_CLUSTER=mt1
